{
  "name": "Fmap",
  "description": "",
  "version": "1.0.0",
  "author": "Landris18",
  "license": "MIT",
  "main": "./server.ps1",
  "scripts": {
    "install": "yarn install --force --ignore-scripts --modules-folder pode_modules",
    "build": "psake",
    "test": "invoke-pester ./tests/*.ps1",
    "start": "./server.ps1"
  }
}
